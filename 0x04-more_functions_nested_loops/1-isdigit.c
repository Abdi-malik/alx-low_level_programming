#include "main.h"
/**
* _isdigit - checks whether c is digit or not
* @c: is holding input that is processed _isdigit
* Return: retuns something that it is value is 1 or 0
*/

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
