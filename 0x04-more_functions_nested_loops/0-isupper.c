#include "main.h"
/**
* _isupper - checks upper case letter
* @c: it will display 1
* Return: if the Letter be Alphabet upper 1 if it is not it returns 0
*/

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
